
import 'package:flutter/material.dart';

class HomeTechnicalScreen extends StatelessWidget {
  const HomeTechnicalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
