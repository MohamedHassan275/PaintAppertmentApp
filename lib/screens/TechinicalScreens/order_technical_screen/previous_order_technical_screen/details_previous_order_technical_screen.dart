
import 'package:flutter/material.dart';

class DetailsPreviousOrderTechnicalScreen extends StatelessWidget {
  const DetailsPreviousOrderTechnicalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
